# Railway uses railway.toml for configuration
# This Procfile is kept for compatibility but railway.toml takes precedence
web: cd beautiful_gradient_mcp && uvicorn main:app --host 0.0.0.0 --port $PORT
